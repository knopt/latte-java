Assembly x86_64 compiler for `src/Latte.cf` grammar. 

To run:
```
make
./latc_x86_64 input.lat
```

`make` compiles Java sources
`latc_x86_64` compiles `input.lat` to assembly x86_64 `input.s`, compiles `lib/runtime.c`, creates executable `input` with linked `lib/runtime.o`

Dependencies: `JLex`, `java-cup`, `java-cup-runtime`, `commons-lang`.

Compiler supports:
- arrays
- structs
- objects
- interfaces
