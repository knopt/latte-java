package src.Absyn; // Java Package generated by the BNF Converter.

import java.util.function.Function;

public class Mprivate extends Modifier {
    public int line_num, col_num, offset;

    public Mprivate() {
    }

    public boolean equals(Object o) {
        if (this == o) return true;
        if (o instanceof Mprivate) {
            return true;
        }
        return false;
    }

    public int hashCode() {
        return 37;
    }

    public <T> T match(Function<Mfinal, T> mFinal,
                       Function<Mpublic, T> mPublic,
                       Function<Mprivate, T> mPrivate,
                       Function<Mstatic, T> mStatic,
                       Function<MEmpty, T> mEmpty) {
        return mPrivate.apply(this);
    }
}
